[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lxcharset"
version = "0.1.0"
description = "LxCharset core package for custom encoding detection"
requires-python = ">=3.10"
